# Runtime dependencies (included in Lambda deployment package)
boto3>=1.34.0
pydantic>=2.6.0
aws-lambda-powertools>=2.35.0

# Development / test dependencies (NOT in Lambda package)
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0
ruff>=0.3.0
mypy>=1.9.0
boto3-stubs[bedrock-runtime]>=1.34.0
